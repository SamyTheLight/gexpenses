function sumatorio(deudaDeCadaUsuario){
    let sumatorio = 0;
    //Sumarlo todo en el sumatorio
    return sumatorio;
}

function comprobarCantidad(totalGasto, sumatorioDeuda){
    return totalGasto -sumatorioDeuda;
}

//Si comprobarCantidad == 0, ya se puede mandar el formulario

//Funcion en javascript que te valide el formulario, obteniendo de los INPUTS las cantidades del sumatorio y pasarselas a la función sumatorio

//eventos en los inputs para validar que metes un número entero o un decimal de 2 posiciones
//calcular el sumatorio de todos los inputs a la vez

//en el php anteriomente 1 consulta para el los datos del pago, 1 consulta para obtener el array de los los usuarios del pago
//for o un foreach -> input hidden name=id_usuario[], label nombre del usuario ,input name=deuda[]